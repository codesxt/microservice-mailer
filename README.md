# Mailer Microservice

Basic Microservice to send Mails

# Instructions

Define the following environment variables:
``` bash
FROM_EMAIL
FROM_NAME
SMTP_HOST
SMTP_PORT
SMTP_SECURE
SMTP_USER
SMTP_PASSWORD
```
